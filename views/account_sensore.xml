<odoo>
    <record id="view_account_sensore_kanban" model="ir.ui.view">
        <field name="name">account_sensor.kanban</field>
        <field name="model">account.sensore</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="nome" widget="kanban_header" />
                <field name="valore_decimale_libero" widget="goug" />
                <field name="read_value" widget="button" icon="fa-eye" />
            </kanban>
        </field>
    </record>
</odoo>